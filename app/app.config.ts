export default defineAppConfig({
  ui: {
    colors: {
      primary: "cyan",
    },
    input: {
      slots: {
        root: "w-full",
      },
    },
    select: {
      slots: {
        base: "w-full",
      },
    },
    textarea: {
      slots: {
        root: "w-full",
      },
    },
  },
});
