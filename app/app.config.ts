export default defineAppConfig({
  ui: {
    colors: {
      primary: "cyan",
    },
    input: {
      slots: {
        root: "w-full",
      },
    },
    textarea: {
      slots: {
        root: "w-full",
      },
    },
  },
});
